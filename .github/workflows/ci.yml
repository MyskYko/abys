name: ci

on:
  push:
  pull_request:

env:
  SLANG_RELEASE_TAG: slang-ubuntu-2026-06-29
  SLANG_TARBALL_SHA256: 86b0eee26eaac399803ec1392bd2d96007d555d137466a066aaded69d30aa892

jobs:
  build-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler:
          - gcc
          - clang
    steps:
      - uses: actions/checkout@v7
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format clang-tidy
      - name: Cache slang (prebuilt)
        uses: actions/cache@v4
        with:
          path: _slang
          key: ${{ env.SLANG_RELEASE_TAG }}-${{ env.SLANG_TARBALL_SHA256 }}
      - name: Fetch slang (prebuilt)
        run: |
          if [ -d _slang/lib/cmake/slang ]; then
            echo "Using cached slang package"
            exit 0
          fi
          mkdir -p _slang
          curl -L -o slang-ubuntu-latest.tar.gz \
            https://github.com/MyskYko/slang/releases/download/${SLANG_RELEASE_TAG}/slang-ubuntu-latest.tar.gz
          if [ -n "${SLANG_TARBALL_SHA256:-}" ]; then
            echo "${SLANG_TARBALL_SHA256}  slang-ubuntu-latest.tar.gz" | sha256sum --check
          fi
          tar -xzf slang-ubuntu-latest.tar.gz -C _slang
      - name: Configure
        run: |
          if [ "${{ matrix.compiler }}" = "clang" ]; then
            export CC=clang
            export CXX=clang++
          else
            export CC=gcc
            export CXX=g++
          fi
          cmake -S . -B build -Dslang_DIR="$PWD/_slang/lib/cmake/slang"
      - name: Build
        run: cmake --build build
      - name: Test
        run: ctest --test-dir build --output-on-failure
      - name: Format check
        run: ./scripts/format_check.sh

  clang-tidy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-format clang-tidy
      - name: Cache slang (prebuilt)
        uses: actions/cache@v4
        with:
          path: _slang
          key: ${{ env.SLANG_RELEASE_TAG }}-${{ env.SLANG_TARBALL_SHA256 }}
      - name: Fetch slang (prebuilt)
        run: |
          if [ -d _slang/lib/cmake/slang ]; then
            echo "Using cached slang package"
            exit 0
          fi
          mkdir -p _slang
          curl -L -o slang-ubuntu-latest.tar.gz \
            https://github.com/MyskYko/slang/releases/download/${SLANG_RELEASE_TAG}/slang-ubuntu-latest.tar.gz
          if [ -n "${SLANG_TARBALL_SHA256:-}" ]; then
            echo "${SLANG_TARBALL_SHA256}  slang-ubuntu-latest.tar.gz" | sha256sum --check
          fi
          tar -xzf slang-ubuntu-latest.tar.gz -C _slang
      - name: Configure
        run: |
          export CC=clang
          export CXX=clang++
          cmake -S . -B build -Dslang_DIR="$PWD/_slang/lib/cmake/slang"
      - name: Clang-tidy
        run: ./scripts/run_clang_tidy.sh build/compile_commands.json
